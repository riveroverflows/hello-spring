spring:
  datasource:
    url: jdbc:h2:~/data/hello-spring;AUTO_SERVER=true
    username: sa

---

spring:
  config:
    activate:
      on-profile: test
  datasource:
    url: jdbc:h2:mem:test
    username: sa
